import { DefaultTheme } from "styled-components/native";

import { miamiThemeColors, miamiThemeFonts } from "./miami";

export const theme: DefaultTheme = {
  colors: miamiThemeColors,
  fonts: miamiThemeFonts,
};
